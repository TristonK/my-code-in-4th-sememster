#include <bits/stdc++.h>
using namespace std;

int n,m;

void init(){
    cin.sync_with_stdio(false);
    cin.tie(NULL);
    cin.exceptions(cin.failbit);
    cin >> m >> n;
}

int main(){
    init();
    return 0;
}