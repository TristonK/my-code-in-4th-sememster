#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.sync_with_stdio(false);
    cin.tie(NULL);
    cin.exceptions(cin.failbit);
   int cnt=0;
   int test[100];
   memset(test,0, sizeof(test));
   test[cnt++]=1<<1;
   cout<<test[0]<<endl;
   return 0;
}